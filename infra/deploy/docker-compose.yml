
services:
  backend:
    container_name: deploy-backend
    image: "${DOCKER_USERNAME}/medicare-backend-app:latest"
    restart: always
    env_file:
      - .env  # 환경 변수 관리 (DB 정보, JWT 등)
    volumes:
      - logs:/app/logs  # 로그 데이터 유지
    networks:
      - medicare_network
    ports:
      - "8080:8080"

volumes:
  logs:  # 로그 볼륨 추가

networks:
  medicare_network:
    driver: bridge